PROGRAM := ITEM*
ITEM := DEF
DEF := def IDENT = TERM
TERM := TERMPART+ | match TERM PAT*
TERMPART := VAR | LAMBDA | LET | HOLE | ( TERM )
PAT := with IDENT+ => TERM
HOLE := ? | ?{...}
IDENT := x, y, z, ...
VAR := IDENT
APP := TERM TERM
LIT := 0, 1, ...
LAMBDA := fn IDENT => TERM
LET := let IDENT = TERM in TERM
